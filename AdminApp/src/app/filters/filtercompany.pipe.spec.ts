import { FiltercompanyPipe } from './filtercompany.pipe';

describe('FiltercompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
